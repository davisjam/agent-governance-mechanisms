# HANDOFF — agent-governance-mechanisms

Full-context continuity doc. Assume the reader (human or model) has **only this repo** and no other
memory. This doc + [`CLAUDE.md`](CLAUDE.md) + [`README.md`](README.md) is everything you need to operate.
This file is **not published** (excluded from `catalog.py build`), so write freely here.

---

## 1. What this repo is

A **standalone public repo** — a pattern catalogue of the **governance mechanisms** for
*governance-centric agentic software engineering*: the controls that let a fleet of AI coding agents stay
productive while bounding their failures. Each mechanism is a Gang-of-Four-style design-pattern writeup.

- Distilled from a 12-week first-person **case study** (the paper: `https://arxiv.org/pdf/2607.01087`,
  *Cheap Code, Costly Judgment*) of building the **ScholAccess** document-accessibility system with
  frontier coding agents. The live system: `https://scholaccess.com`.
- **Published** as a static GitHub Pages site: `https://davisjam.github.io/agent-governance-mechanisms/`.
- **Embedded** in a private parent repo (`ada-tool`) as a **git submodule** at
  `talks-and-notes/governance-catalog`. This repo is the origin `git@github.com:davisjam/agent-governance-mechanisms.git`.
- The parent repo (`ada-tool`) is the real system these mechanisms were extracted from; it is **private
  and NOT shipped**. That fact drives the governing principle below.

## 2. Governing principle (the load-bearing rule)

**This repo must be interpretable by an independent Claude** — an agent with no access to `ada-tool`, its
source, docs, tools, or CLAUDE.md rule numbers. Every entry must stand on its own. The full rule and the
test are in [`CLAUDE.md`](CLAUDE.md) §"Core principle: this repo must be interpretable by an independent
Claude". **This was the largest open work item; it is now resolved** via the abstractions glossary — see
§9 for the mechanism.

## 3. Current state (verify, don't trust)

- **53 entries · 3 roles** — agent 22 · models-bridge 11 · product 20 · **11 families**.
- **18 abstractions** in [`ABSTRACTIONS.md`](ABSTRACTIONS.md) — the glossary every entry cites in place of
  unshipped filenames (see §9). All 53 entries are migrated onto it; the ban is global.
- Verify anytime: `python3 catalog.py validate` → must print `… — 0 issue(s)`. `python3 catalog.py build`
  → `built 61 entry/index pages …`.
- The parent repo pins this repo's HEAD via its submodule pointer, bumped after every push (see §6).
- Pages deploys automatically on push to `main` via `.github/workflows/pages.yml` (Pages **source =
  GitHub Actions**). Historical note: there was a GitHub-wide Pages incident during initial setup — if a
  deploy hangs in `deployment_queued`, check `githubstatus.com`; it's not this repo.

## 4. Repository layout

```
README.md              public intro: the "two schools" table + two-mechanisms + links
CLAUDE.md              agent guide + the interpretability principle (READ FIRST if editing)
HANDOFF.md             this file (not served)
INDEX.md               the census: role → family → one row per control
ABSTRACTIONS.md        the glossary: concrete artifacts named by role+shape; entries cite [[slug]] (§9)
abstractions-playbook.md  internal how-to for the glossary (when to [[cite]] vs inline); NOT served
TODO.md                deferred work (the single-machine-contention rephrasing); NOT served
catalog.py             the ONE tool (stdlib-only). validate/query/summaries/build/install-hooks/deploy
quick-start.md         "adopt these in your repo" guide
index.html             GENERATED landing (do not hand-edit)
catalogue-views.html   GENERATED metadata-driven views (do not hand-edit)
catalogue-figure.html  HAND-AUTHORED control-map figure (4 views, clickable nodes)
development-workflow.html  HAND-AUTHORED dev-process figure (verbatim, ScholAccess-flavoured)
.nojekyll              disables Jekyll on Pages
hooks/pre-commit       regenerates + stages HTML on commit (install: catalog.py install-hooks)
.github/workflows/pages.yml   CI: validate + build + deploy Pages on push
downloads/CLAUDE-starter.md   redacted CLAUDE mirror (a download; NOT rendered)
agent/  models-bridge/  product/    the 53 entries as <role>/<family>/<control>.md (+ generated .html)
```

- Roles/dirs: `agent/` (govern the fleet), `models-bridge/` (the typed MBSE models bridging agents and
  code), `product/` (the shipped artifact — flagged ScholAccess-specific).
- **Every `.html` except the two hand-authored figures is GENERATED** by `catalog.py build` and carries a
  `GENERATED by catalog.py build — DO NOT EDIT` banner. Edit the `.md`, never the `.html`.
- `downloads/`, and the internal `HANDOFF.md` / `abstractions-playbook.md` / `TODO.md` (the `NOSERVE`
  set in `catalog.py`), are excluded from both rendering and link-checking. `catalog.py` also scopes its
  markdown discovery to the catalogue trees (root + role dirs), so an untracked packaged-skill copy in
  `plugin/` is ignored.

## 5. `catalog.py` — the tool (stdlib-only, no pip)

| Command | Does |
|---|---|
| `python3 catalog.py validate` | schema + INDEX-consistency + link-integrity + summary checks; exit 1 on any violation |
| `python3 catalog.py build` | render every `.md` → sibling `.html`; regenerate `index.html` + `catalogue-views.html` |
| `python3 catalog.py query [--role --family --form --enf] [--json]` | list/filter entries |
| `python3 catalog.py summaries [--json]` | dump role/family/entry summaries |
| `python3 catalog.py deploy local [--port N]` | validate + build + serve at `http://127.0.0.1:8137/` |
| `python3 catalog.py deploy github [-m msg]` | validate + build + commit + push (triggers Pages) |
| `python3 catalog.py install-hooks` | `git config core.hooksPath hooks` (auto-regen on commit) |

`catalog.py` contains a **compact, dependency-free markdown→HTML renderer** — it handles headers, tables,
lists, code spans/fences, bold/italic, links (rewrites `.md`→`.html`). It is deliberately not a full
markdown engine: **avoid `<br>` and bold-with-nested-italic (`**a *b* c**`) in `.md`** — they don't render
correctly. (BSD `sed` lacks `\b`; use `perl -pi -e` for word-boundary edits.)

## 6. Workflow to make and ship a change

1. Edit the `.md`.
2. `python3 catalog.py validate` (must be **0 issues**) → `python3 catalog.py build`.
3. Preview: `python3 catalog.py deploy local` → open `http://127.0.0.1:8137/`.
4. Publish: `python3 catalog.py deploy github -m "..."` (commits here + pushes → Pages deploys on push).
5. **Bump the parent pointer.** In `ada-tool` (the parent), the submodule gitlink must be advanced to the
   new public HEAD. Parent policy forbids the orchestrator committing on `main` directly, so this is done
   by a **commit-slave** sub-agent. The canonical commit-slave brief:
   - `export ADA_TOOL_AGENT_ROLE=commit-slave`
   - `git add talks-and-notes/governance-catalog` (ONLY the gitlink)
   - `python3 tools/agents/agent_commit.py --of 1/1 -m "chore(governance-catalog): bump submodule -> <sha> (...)"`
   - This is a parent-repo mechanic; from *inside* this repo you only ever `deploy github`.

The tracked `hooks/pre-commit` (install once via `catalog.py install-hooks`) re-runs validate+build+stage
on every commit, so committed HTML can never drift from the `.md`.

## 7. Schema (what `validate` enforces)

Each entry `<role>/<family>/<control>.md`:

- `# Title`, then `**Intent** — …`.
- A **metadata card** — exactly these rows, in this order:
  ```
  | | |
  |---|---|
  | Summary | <=100-char one-liner (shown as page subtitle + census column) |
  | Target | Agent|Bridge|Product · **<Family>** |
  | Form | `<one of nine>` |
  | Enforcement | **Hard**|**Soft**|**Soft·Hard** (…) |
  ```
  *(Novelty, Real artifact, Governing rule(s) were removed from the schema; Summary is first.)*
- **Form** ∈ {`typed-ir`, `validation`, `repair-vocab`, `agent-output`, `bounded-service`, `regression`,
  `quality-gate`, `observability`, `audit-trail`}. **Enforcement class** ∈ {Hard, Soft, Soft·Hard}.
- Seven `##` sections, in order: `Motivation` · `Why it's not just …` · `Mechanism` · `Prerequisites` ·
  `Consequences & costs` · `Known uses` · `Related controls`.
- `Related controls` bullets carry a **relationship tag** (Counterpart / Enabler / Layer / Consumer /
  Bridge / See also) and link other entries via relative `../<role>/<family>/<control>.md`.
- **INDEX.md** has one row per entry; its `Form` and `Enforcement` **must match** the entry's card
  (validated). INDEX row shape: `| ✅ | <Control name> | \`<form>\` | <Enf> | [<file>.md](<path>) |`.
- The umbrella `README.md` and the three role READMEs each carry a `<!-- summary: … -->` comment (used by
  `summaries`/tooltips and checked by validate).

## 8. What the generated site contains

- **`index.html` (landing)** — built from `catalog.py` constants: a top-nav (James C. Davis · GitHub);
  a lead; the **"Between two schools of thought"** spectrum (Vibe coding · **Governance-centric (midway,
  highlighted)** · Oversight-centric) with per-school example links; **"Governance has two mechanisms"**
  (Architecture = poka-yoke/impossible-by-construction; Control = observe/guard); the embedded
  dev-process figure; a `<hr>`; **"Walking through the loop"** (a 4-step flow + an SVG return arrow + a
  "Governability compounds" outcome + "two paired concepts" bullets); action cards; and the **census**
  (grouped `Governance: Agents / Models — a bridge… / Product`, columns **Control · Summary ·
  Enforcement**, representative-not-exhaustive, with a ScholAccess-specific note above the Product role);
  a site footer (© James C. Davis, ECE @ Purdue + GitHub). To change the landing, edit the constants in
  `catalog.py` (LANDING_INTRO, SCHOOLS, WAYS, LANDING_CARDS, `_landing_flow`, `build_census`, the CSS
  blocks), not `index.html`.
- **`catalogue-views.html`** — a metadata-driven view page: `CARDS` emitted from entries, `renderForView`
  + swappable groupings (by role&family / enforcement / form).
- **The two figures are hand-authored HTML** — edit them directly (they are not regenerated). Figure
  control-nodes are wired as links to entry pages via ad-hoc scripts run once at authoring time.

## 9. The abstractions glossary (interpretability — RESOLVED) + open items

- **[RESOLVED — the interpretability debt].** The debt that used to head this list (unshipped filenames
  and bare rule-number citations that an outside Claude can't resolve — §2) is **fixed** across all 53
  entries. The mechanism, and the ongoing discipline, is the **abstractions glossary**:
  - **[`ABSTRACTIONS.md`](ABSTRACTIONS.md)** — a single-page glossary. Each entry is `## Headword`
    (the abstract role) + `<!-- slug: … -->` + a definition + a `**Grounds** — …` line naming the real
    artifact **once** + a `**See** — [control](…)` link. The `Grounds` line is the *only* place a raw
    parent filename appears in the served site. 18 entries.
  - **How entries cite it:** `[[slug]]` (renders the headword) or `[[slug|custom text]]`. Rendered as a
    dotted-underline link to `ABSTRACTIONS.html#slug` with the definition as a hover tooltip.
  - **The decision procedure** (filename → glossary if non-obvious & recurring, else self-describing
    prose; the artifact's own control page uses prose, not a self-citation; rule numbers ALWAYS become
    their content, never a glossary entry) is written down in
    [`abstractions-playbook.md`](abstractions-playbook.md) (internal, not served). **Read it before
    editing or adding an entry.**
  - **The gate is global.** `catalog.py validate` now fails on ANY entry that carries a backticked
    *unshipped* filename (basename absent from this repo) or a bare `#NN` rule cite — so the debt cannot
    creep back, and a new entry is forward-policed automatically.
  - Reproduce the (now-clean) audit:
    `grep -rlE '\`[A-Za-z0-9_./-]+\.(py|cs|jsonl|ya?ml)\`' agent models-bridge product` → no output.
- **[OPEN — single-machine-contention framing].** All development was on one machine (maximal contention,
  minimal cost); a few mechanism descriptions (the mediator/`flock` ones especially) should be lightly
  rephrased to distinguish the general "arbitrate contention" mechanism from our single-host `flock`
  instantiation. Captured in [`TODO.md`](TODO.md). Deferred.
- The two figures are manual; keep them in sync with schema changes by hand.

## 10. Locked decisions (don't re-litigate)

- `catalog.py` stays **stdlib-only** (clone-and-run; no pip). It is the single source that builds the site.
- Fonts: **Source Serif 4** (headings) + **Source Sans 3** (body), from Google Fonts. Layout:
  `main { width:94vw; max-width:1320px }`; prose classes capped ~840px for readability; census/grids/
  figure use full width.
- The catalogue is **representative (~53 patterns), not exhaustive** of every lint/gate/test.
- The **`product/` role is flagged ScholAccess-specific** (a note renders above it in the census).
- Redaction: the product name is scrubbed everywhere; **scholaccess.com** and the **paper** are the two
  deliberate outward links.
- Schema slimmed to **Summary · Target · Form · Enforcement** (Summary first); Novelty / Real artifact /
  Governing rule(s) removed. Census shows **Control · Summary · Enforcement** (no Form column, no ★, no
  hover tooltip). The event-bus mechanism is titled **"Orchestrator-as-reactor over an event bus"**.
- The two newest mechanisms (both in the governance-doc family): **Operational playbooks** and **Epic &
  design-doc templates**.
- **Abstractions are a single page**, not a folder of per-term files — a glossary is one scan-and-hover
  surface. Entries cite it via `[[slug]]`; the headword is the abstract *role*, never the filename. Rule
  numbers are always de-referenced inline to their content (too unstable/unhelpful to be glossary terms).

## 11. History (in the PARENT ada-tool repo, not here)

- `talks-and-notes/PLANNED-ORDER-OF-WORK-catalog-site.md` — the phased build plan (0 schema-tool · 1
  submodule+Pages · 2 tooltips · 3 md→html codegen · 4 landing · 5 views/4+1 · 6 CI).
- `talks-and-notes/HANDOFF-catalog-site-260702.md` — the original pre-compact build handoff.
- These are parent-repo artifacts; you won't have them from inside this repo — they're listed for the
  human who has both.
