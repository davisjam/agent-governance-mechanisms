# READY-TO-DISPATCH BRIEF — TASK 6 "MAGE follows from the machine" (Fable-led, models-first)

Dispatch AFTER the core-concepts pass (DONE, live `8a92395`). Model **fable-5**. run_in_background. Live tree, branch main, NO worktree, single writer. commit-early-often (per sub-item). Deploy at end (or incrementally). Record to the results-log.

## Brief text (paste into Agent prompt)

You are editing the MAGE book in the governance-catalog repo (LIVE checkout at `/Users/davisjam/Projects/ada-tool/talks-and-notes/governance-catalog`, branch main — NO worktree, you are the only writer). Work **models-first-then-prose**, **slow and correct over fast and lossy**, **commit early and often** (per sub-item, descriptive messages). This is NOT the parent ada-tool product — ignore its dispatch/lint/worktree machinery; the book workflow is edit → `catalog.py validate` → `book/build_book_html.py` → `catalog.py deploy github`.

**Your task = TASK 6, spec'd VERBATIM in** `book/_design/book-editorial-discipline-directive-260802.md` **§ "TASK 6 — MAGE FOLLOWS FROM THE MACHINE"** (sub-sections 6.1–6.5). Read it in full — it carries the three property groups, the 9-row table + caption, and the author-drafted opening passage. Its thesis: **MAGE is true engineering of a NEW MATERIAL (foundation models + agentic harnesses) whose properties we explain and build on — NOT the "LLM as fallible teammate + analogy" move.** Proceed from PROPERTIES + the MECHANISMS that turn them into value. No fallible-teammate analogies. Keep the N=1 humility intact (DocAble is a grounding case, never a proof).

**Context you build on (just landed, live):** the CORE-CONCEPTS PASS added a six-level concept-model `_hierarchy` + six `core-construct` records + the two theses as relations to `book/data/concepts.json`, reconciled to the 14-claim `book-models/argument-spine.json`, plus formal defs (Model/Churn/engineered-Loop/Governance-mechanism) in `book/data/definitions.json`. Task 6 EXTENDS this: it supplies the *derivation* — substrate properties → engineering consequences → the two theses + the GEE. So the theses/GEE that the concept model currently states, Task 6 now shows FOLLOW FROM the machine.

**MODELS-FIRST execution:**
1. **Model the derivation FIRST.** Add the substrate-properties → consequences → thesis/GEE-derivation to the model — either extend `book/data/concepts.json` `_hierarchy` (a `substrate_derivation` block: the foundation-model + harness properties, each linked to the consequence and the thesis/construct it derives) OR a small new `substrate-properties` model sibling if that's cleaner. Cross-reference the argument spine (claims: abundant-implementation, oversight-does-not-scale, govern-the-environment, modeling-thesis, alignment-thesis) — the derivation is the *why* under those claims. Keep any new audit-only check consistent with the existing L1–L5 pattern (audit-only-first per the repo's blocking-lint-landing discipline). Regenerate reverse_index/outline if touched.
2. **6.1 — NEW Part-1 unit "Why MAGE Follows from the Machine"** (~4–6pp; tight, NOT a sprawling taxonomy). Placement: in Part 1 as the DERIVATION of the two theses — after the printer/loops material, at or just before where the theses are finalized. **Apply the section-word-cap discipline** (a single section can't hold 4–6pp; structure it as a short chapter with tight internal sections, the way 1.2 was handled — renumber cleanly and update ALL cross-refs + landing/outline/outcomes anchors if you add a chapter). Three property groups (foundation-model · harness · derived consequences) then CONCLUDE with the thesis+GEE derivation (6.1's bullet). Include **6.4 the 9-row TABLE** (property → problem/opportunity → MAGE response) with the caption verbatim, overflow-clean (figure-fit runbook if it's rendered as a figure; else a clean markdown table). Open with **6.5's passage adapted to house voice** (self-communicate — don't paste raw; match the voice/rhetoric files).
3. **6.2 — strengthen Part 3 opening** ("why are models newly central NOW?" — the three-way economic argument: agents NEED models · CAN maintain them · harnesses can ENFORCE them). PROMOTE the executable-source-of-truth architecture (agents read the model · generators consume it · drift gates block divergence) from a catalogue entry INTO the main Part-3 chapter prose.
4. **6.3 — strengthen Part 2 opening** (derive governance from the joint substrate; the "A language model alone… A harness turns some into actions… A governed engineering environment interposes…" framing; then soft-conditioning · hard-authority · feedback). Promote the existing "soft guidance enters, deterministic guardrails constrain action, a deterministic envelope governs admission" formulation to more prominence.

**House style:** this repo's CLAUDE.md "Writing style" (Hemingway; active voice; short sentences; cut qualifiers; vary rhetorical figures, cap em-dash density). Keep book coverage ⊇ site framings. Honor the claims-model C7 watch-phrase discipline on any strong new claim.

**GATES (before each deploy):** `python3 catalog.py validate` (0 issues) · `python3 book/build_book_html.py` (green) · `python3 catalog.py deploy github`. Then VERIFY LIVE: foreground-poll the Deploy Pages CI run to conclusion=success (do NOT declare done on push alone), curl the landing + touched chapters + the PDF for 200. Deploy stages TRACKED changes only — leave the untracked `book/_design/*.md` drafts untracked.

**RECORD** (do not relay): append a `## TASK 6 — MAGE follows from the machine` block to `book/_design/editorial-run-results-260802.md` — the model change (derivation), the new/renumbered Part-1 unit + table + placement, the Part-2/Part-3 promotions, cross-ref/anchor updates, gates, live SHA(s), and any CUTS/MERGES.

Be thorough over fast. On a load-bearing ambiguity you can't resolve from the directive + models, make the most defensible call, DOCUMENT it in the results-log, and continue.
