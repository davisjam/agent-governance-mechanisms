# READY-TO-DISPATCH BRIEF — CORE-CONCEPTS PASS (Fable-led, models-first)

**Dispatch AFTER Phase 1 fully lands** (argument-spine deployed + CI green) so this can reconcile with the landed spine. Model: **fable-5**. run_in_background: true. Single live tree (NO worktree) — this is the only live-tree writer while it runs (1-agent-max). commit-early-often (per-item) so a death is recoverable.

## Brief text (paste into Agent prompt)

You are editing the MAGE book in the governance-catalog repo (LIVE checkout at
`/Users/davisjam/Projects/ada-tool/talks-and-notes/governance-catalog`, branch main — NO worktree; you are the only writer). Work **models-first, then prose**, **slow and correct over fast and lossy**, and **commit early and often** (one commit per item, descriptive messages) so any interruption is recoverable.

**FIRST, read these (verbatim spec):**
- `book/_design/book-editorial-discipline-directive-260802.md` — the section **"PUNCH-LIST ITEM — the CONCEPT MODEL … CAPSTONE"** and the five sibling PUNCH-LIST items above it (Modeling-Thesis broaden — ALREADY DONE in the spine, verify only; formal **Model** def; **Churn** def; **engineered LOOP** + diagram; **Governance mechanism** + 4 classes). The `### ⇒ These SIX items are ONE foundational CORE-CONCEPTS PASS` paragraph is your task list + sequencing.
- `book-models/argument-spine.json` (the landed Phase-1 spine — 14 ordered claims). The concept-model hierarchy IS the same object at a coarser grain; **reconcile them** (don't contradict; cross-reference).
- `book/data/definitions.json` (green-box defs SSOT — keyed dict; `model` term exists) and `book/data/concepts.json` (concept-card registry — `kind`∈{thesis,axis,family,mechanism-class,caveat}, `status`; join key = the `- concept:` registry in `book/index-terms.md`; `name`/`book_home` are DERIVED, never stored).

**THE SIX ITEMS (models-first each; the concept model is the capstone that frames the other five):**
1. **CONCEPT MODEL (capstone).** In `book/data/concepts.json`, model the explicit HIERARCHY (SSOT for the book's conceptual structure): Premise (implementation abundant, judgment scarce) → Failure mode (churn) → Two theses (Modeling · Alignment) → Method (governance-conversion loop) → Resulting system (governed engineering environment) → Construction repertoire (the catalogue: models · mechanisms · compositions · known uses). Name the **six CORE CONSTRUCTS** — agent loop · model · governance mechanism · churn · governance-conversion loop · governed engineering environment — with the **two theses as the RELATIONS** among them. Add whatever structure concepts.json needs (a `_hierarchy`/`spine` block or ordered `kind` entries) WITHOUT breaking its existing derive-at-check-time contract or the audit-only L1–L4 lints. Reconcile with `argument-spine.json`.
2. **Formal Model def.** Update `definitions.json` `model.box` to: *"A deliberately reduced, machine-readable representation of system intent, structure, behavior, policy, or evidence that supports reasoning, derivation, or checking, and whose relationship to the implemented or observed system is mechanically maintained."* KEEP the prose/code/middle intuition alongside. Propagate to Part 3 (`3.1-the-executable-zoo`, `3.7` scope-of-modeling) + everywhere "model" is defined/leaned-on.
3. **Churn def.** Canonical: *"The condition in which an increasing share of agent effort is spent rediscovering context, undoing recent changes, repairing regressions, or reconciling inconsistencies rather than advancing the system."* Update its SSOT (definitions.json/concepts.json + claims model if homed) then the preface where churn is introduced. Keep `churn-is-symptom` consistent (churn = symptom; the three not-knowings = causes).
4. **Engineered agent LOOP def + elaborate the diagram.** Def: *"a feedback process in which the environment supplies a modeled state, constrains available actions, measures progress against an explicit criterion, and determines whether the result may advance."* Foreground the **FOUR engineered levers** — state exposed · action surface · success criterion · admission rule — connecting modeled-state→Modeling-Thesis and constrained-actions+criterion+admission→Alignment-Thesis. Update the loop concept in the model, then ch `1.3-loops-and-models` prose, then **redraw `book/assets/loop-engineering.svg`** (house style, overflow-clean via the figure-fit runbook) so the four levers are the engineer's control points — distinctive to MAGE, not a generic OODA/control loop.
5. **Governance-mechanism def + 4 classes.** Def: *"A repeatable environmental structure that encodes an engineering policy and either constrains an action, detects a violation, requires evidence, or controls admission."* Four classes: **prevention · detection · evidence · admission.** Load-bearing: *a model is NOT a governance mechanism until something reads or enforces it.* Add to definitions.json + the Alignment-Thesis statement/claim. Note the relation to the catalogue's existing **Move** axis (constraint/sensor/package).
6. **Rename "primitive" → core construct / foundational construct.** RESERVE "primitive" for implementation-level code ops (raw calls/strings/mutations/traversals — the "dense primitive region" DEPENDS on this meaning). For conceptual units use **core concepts / foundational constructs**. Grep the LIVE book prose (NOT node_modules, NOT archival transcripts unless they're rendered): confirmed live sites include `book/index-terms.md`, `book/part3/3.3-the-process-view.md`, `book/part4/4.1-brownfield.md`, `book/part5/5.2-the-timeline-and-the-work.md`, `book/part2/2.5-metrics.md`. Disambiguate each carefully — some are legitimately code-level (keep "primitive"), some are conceptual (rename). Judgment per-site.

**TWO PHASE-1 FOLLOW-UPS folded in (you touch the spine anyway):**
- **Ratify `1.2-mage-by-example` as `overview`-exempt** in `book-models/argument-spine.json` (its declared source + regen): it advances 5 claims by design (whole-book teaser). Add it to the exempt set with reason `overview` (the enum already carries the reason; ratify-before-use). Re-run the drift gate; keep it audit-only-first.
- **Close the claims-5+9 body-coverage gap.** Spine claims **5 (three-not-knowings-cause-churn)** and **9 (theses-divide-the-not-knowings)** are currently advanced ONLY in the exempt preface. Since this pass formalizes **churn** (item 3) and the **two theses** (items 1/5), ensure a BODY chapter develops one of them by name — natural homes: `1.3-loops-and-models` (the not-knowings frame) and/or `2.3-the-governed-environment` (theses→causes mapping). Update the chapter's spine labels in the model to match the prose you add. Light touch — a paragraph that names the frame, not a rewrite.

**GATES before each deploy:** `python3 catalog.py validate` (0 issues) · `python3 book/build_book_html.py` green · then `python3 catalog.py deploy github`. Verify live after. If you add a claims/definition entry, honor the C7/watch-phrase discipline. Keep a CUTS/MERGES note if you cut or merge anything.

**RECORD your outcome** by appending a `## CORE-CONCEPTS PASS` block to `book/_design/editorial-run-results-260802.md` (not a chat relay) — what you changed in each model, the prose sites propagated, the figure redraw, the rename decisions (kept-vs-renamed per site), gates, live SHAs.
