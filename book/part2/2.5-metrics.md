<!-- part-title: The Governed Engineering Environment -->
<!-- chapter-title: Metrics -->

# Metrics: The Sensing Half of the Loop

<!-- point: a-metric-is-the-sensing-half-of-the-agent-loop | Every agent runs a loop — input, reason, act, fold the result back around — and this chapter develops the sensing half of it, because a loop with no honest sense of its own state cannot steer at all. -->
Every agent runs a loop. It takes an input, reasons, acts, and folds the result back around.
The chapter on loops said the metric is what the agent steers by — skip it and the agent
searches forever, never knowing when to stop. That chapter spent its words on the acting half.
This one develops the sensing half, because a loop with no honest sense of its own state cannot
steer at all.

<!-- point: the-metric-comes-first-and-must-drive-a-decision | A metric senses, a playbook acts, a mechanism enforces — and the metric comes first because the other two are blind without it: a playbook firing on the wrong number does the wrong work, a mechanism grading on too coarse a number blocks the wrong commit, so the governing question for every metric is not "can I measure this?" but "does this number drive a decision?" -->
A metric senses. A playbook acts. A mechanism enforces. Those three make up a governed loop, and
the metric comes first because the other two are blind without it. A playbook that fires on a
number that measures the wrong thing does the wrong work. A mechanism that grades on a number too
coarse to name the fault blocks the wrong commit. So the question that governs every metric in
this chapter is not "can I measure this?" — almost anything can be counted — but "does this
number drive a decision?"

## Measure one level deeper

<!-- point: measure-one-level-deeper-because-the-surface-number-cannot-act | Ousterhout's rule in one line — measure one level deeper than the number you think you want — because a metric earns its place only when it drives a decision, and the surface number usually cannot. -->
<!-- index-def: measure-one-level-deeper -->
John Ousterhout gives the rule in one line: **measure one level deeper than the number you
think you want.** A metric earns its place only when it drives a decision, and the surface
number usually cannot.

<!-- point: a-cloud-bill-reports-but-splitting-it-by-service-acts | A cloud bill is real and useless: it tells you to pay the invoice, not what to change — but split the same bill by service and one usually dominates while the rest trail off in a thin tail, and the dominant one names your first target, the deeper number carrying a decision the shallow one buried. -->
Take cost. A cloud bill tells you what the whole project spent last month. That number is real,
and useless: it tells you to pay the invoice, not *what to change* — which service to slim, which
call to batch, which cascade to cut. It affords the report, not the engineering. Split the same
bill by service and it starts to act. One
service usually dominates and the rest trail off in a thin tail, and the dominant one names your
first target. You measured one level deeper, and the deeper number carries a decision the shallow
one buried.

<!-- point: boot-time-one-level-deeper-turns-scale-out-into-fix-the-slow-phase | An 8-second boot as one number means only "scale out"; measured one level deeper — 6 of those seconds are one initialization phase warming up — it means "optimize that phase first," the same latency turned from a reflexive scale-out into a specific engineering decision. -->

> ### Worked example — boot time, one level deeper
>
> Say a service takes 8 seconds to boot. As one number it means only *scale out* — add instances
> to hide the wait. Measure one level deeper — 6 of those seconds are the GenAI client warming up,
> the rest a thin tail — and it means *optimize the GenAI-client init first*. Same latency, one
> level deeper, and now the number drives an engineering decision (fix the slow phase) instead of a
> reflexive scale-out. (Illustrative figures.)

<!-- point: line-coverage-is-the-invoice-level-number-and-the-flagship-is-coverage-one-level-deeper | The same trap hides in test coverage: "the suite covers 87% of lines" is the invoice-level headline that reports and does not act, because a line in a throwaway helper counts the same as a line in the job state machine, so the chapter's flagship metric is coverage measured one level deeper — coverage that names the gap instead of hiding it in a percentage. -->
The same trap hides in test coverage. "The suite covers 87% of lines" is the invoice-level
number — a headline that reports and does not act. It cannot tell you *which* untested code
matters, because a line in a throwaway string helper counts the same as a line in the job
state machine. The rest of this chapter walks a spectrum of metrics from the hard,
deterministic end to the soft, judgment-laden one, and the flagship in the middle is exactly
coverage measured one level deeper — coverage that names the gap instead of hiding it in a
percentage.

<!-- point: metrics-run-a-hard-to-soft-spectrum-and-the-mistake-is-treating-one-as-the-other | The metric spectrum tracks the soft-versus-hard governance axis: a hard metric is deterministic — a machine reads it and the same input yields the same number — while a soft metric is judgment-laden, pointing a reader at a place to look without reducing to a self-deciding threshold; both are worth having, and the mistake is to treat one as the other — blocking a build on a soft signal, or leaving a hard fact to a reviewer's memory. -->
The spectrum tracks the book's soft-versus-hard governance axis. A **hard** metric is
deterministic: a machine reads it, and the same input yields the same number every time. A
**soft** metric is judgment-laden: it points a human or an agent at a place to look, but it
cannot be reduced to a threshold that decides on its own. Both are worth having. The mistake is
to treat one as the other — to block a build on a soft signal, or to leave a hard fact to a
reviewer's memory. [ref:metric-spectrum] lays the metrics out from the hard, deterministic end to the soft.

<!-- label: metric-spectrum -->
```mermaid
flowchart LR
    subgraph HARD["HARD · deterministic · machine-read"]
        CS["cold-start latency<br/>ms per service · longest-path Σ<br/>→ fed into the perf model"]
        DIST["component-distance grade<br/>HARD / SOFT / SILENT<br/>→ decides whether a gate fires"]
    end
    subgraph FLAG["FLAGSHIP · the deeper cut"]
        MEC["entry-point-native coverage<br/>is the model CLAIM exercised?<br/>→ names the untested invariant"]
    end
    subgraph SOFT["SOFT · judgment-laden · aims a reader"]
        DDT["doc-derived-test pinning<br/>is this doc-claim pinned by a test?<br/>→ nudges what to test next"]
    end
    HARD --> FLAG --> SOFT
    accTitle: The metric spectrum, hard to soft
    accDescr: A left-to-right spectrum of governance metrics. On the hard, deterministic, machine-read end sit cold-start latency and the component-distance grade. In the middle sits the flagship, entry-point-native coverage, which asks whether a model claim is exercised by a test. On the soft, judgment-laden end sits doc-derived-test pinning, which asks whether a documented claim is pinned by a test. Each metric is labelled with the decision it drives.
```

## The hard end: cold-start latency, machine-read and model-fed

Start at the deterministic end, where a metric is a millisecond number nobody argues about.

<!-- point: cold-start-latency-reads-obviously-as-the-scale-from-zero-penalty | A serverless fleet scales to zero, so a request that wakes a cold instance waits while the container boots and the runtime loads — the cold-start latency — and the obvious reading is the penalty of scaling from zero: measure it, and if it hurts, pay to keep a warm instance running. -->
A serverless fleet scales to zero. When a request wakes a cold instance, the user waits while
the container boots and the runtime loads. That wait is the cold-start latency, and the obvious
way to read it is as the penalty of scaling from zero: measure it, and if it hurts, pay to keep a
warm instance running.

<!-- point: the-deeper-cold-start-number-named-runtime-init-not-scale-from-zero-or-image-size | The obvious reading was wrong: a validator service carried a multi-second warm floor a warm instance did nothing to remove, and the deeper number named its cause — a runtime relaunched once per request paying a fresh startup each time — so the fix was a re-architecture that kept the runtime resident, cutting the warm floor many-fold; the barrier was runtime initialization, not scale-from-zero and not image size, and only the deeper measurement said so. -->
The obvious reading was wrong, and measuring one level deeper is what showed it. The conformance
service — a PDF validator wrapping veraPDF, a Java tool — carried a 4,057 ms *warm* floor, a wait a
warm instance did nothing to remove. The deeper number named its own cause: veraPDF ran as a
subprocess launched once per request, so every call paid a fresh JVM startup. The tax was not
scale-from-zero; it was the process launch, and no warm-instance lever could reach it. The fix was
a re-architecture — keep the runtime resident instead of relaunching it — and the warm floor fell
from 4,057 ms to 109 ms, a thirty-seven-fold cut that took nearly four seconds off every request.
The .NET service told the same story smaller: 494 ms of per-request CLI launch, down to 103 ms once
the runtime stayed resident. And the tempting surface explanation, that the container images were
simply too big, was also wrong — a 433 MB service cold-started fine. The barrier was runtime
initialization, not image size, and only the deeper measurement said so.

<!-- point: spot-checking-doesnt-scale-so-a-model-reasons-over-the-chain-as-a-longest-path-sum | Spot-checking the one service whose latency looked wrong does not scale — it catches the service that draws your eye and says nothing about how services interact — so the better instrument is a model holding every service's numbers and reasoning over the paths between them, because the sharpest question is the chain: a request landing when every downstream service is cold triggers a cascade, and the worst case is a longest-path sum, each step contributing the slowest cold-start it waits on. -->
That is a metric read at the level where the decision lives: resident-runtime versus warm-instance
is a different fork than the shallow cold-start number implied, and a fleet-average number would
have buried it. But I found that tax by hand — spot-checking the one service whose latency looked
wrong — and spot-checking does not scale. It catches the service that happens to draw your eye,
misses the ones that do not, and says nothing about how the services interact. The better instrument
is a model that holds every service's numbers and reasons over the paths between them, so the costly
case falls out of the graph instead of out of a hunch. Because the sharpest question is not any
single service. It is the *chain*. A request that lands when every
downstream service is cold triggers a cascade of cold-starts along the critical path. The
revenue-core journey runs its steps in sequence; within a step, its calls fan out in parallel.
So the worst case is a **longest-path sum**: each step contributes the slowest cold-start it
waits on, and the path total is the sum of those per-step maxima. That number — not the
per-service one, and certainly not the average — is the one that predicts the worst latency a
real user can hit. It is the metric measured at the level where the decision lives.

<!-- point: the-hard-ends-defining-move-is-the-number-feeds-the-model | The hard end's defining move is that the number feeds the model: the cold-start measurements do not live in a dashboard a human reads and forgets — they land as typed fields on the deployment-topology model the fleet reasons through, and a recompute tool reads those fields to derive the longest-path worst case, refining on its own whenever a fresher number is written. -->
Here the hard end shows its defining move: **the number feeds the model.** The cold-start
measurements do not live in a dashboard a human reads and forgets. They land as typed fields on
the deployment-topology model the fleet reasons through — a `cold_start_ms` and a `warm_ms` on
each service's plane record. A recompute tool reads those fields and derives the longest-path
worst case over the journey graph. When a later measurement writes a fresher number onto a
service's annotation, the tool reads it live and the worst-case total refines on its own, no
edit required.

> Learn more about this governance mechanism: [deployment-topology model](appendix-b-deployment-topology-model.html).

<!-- point: automate-the-map-upkeep-into-the-machine-rather-than-a-runbook-reminder | The commit is automated because a runbook step — "after a measurement run, remember to commit the new numbers" — rots and the model drifts; so the pipeline commits the refreshed measurements itself under a housekeeping commit with no human in the loop, building the map's upkeep into the machine that changes the territory rather than asking a person to remember it: architecture over a reminder. -->
The commit is automated. The tempting alternative is a runbook step — *after
a measurement run, remember to commit the new numbers.* A reminder like that rots; someone forgets,
and the model drifts from the fleet it describes. So the pipeline commits the refreshed measurements
itself, under a bypass-prefixed housekeeping commit with no human in the loop, and the model's
cold-start parameters stay current with zero manual action. Build the map's upkeep into the machine
that changes the territory rather than asking a person to remember it — architecture over a reminder.
The runbook note then documents an automated step instead of a chore.

<!-- point: as-built-the-seam-is-in-and-the-data-catches-up | As-built, the live-annotation path is built but not yet populated — the tool runs off a snapshot table of measured values because the fleet is mid-migration — which is the ordinary shape of real work, the seam in and the data catching up; what matters for the chapter is the pattern: a hard metric measured deterministically, written into a typed model, and consumed by a tool that turns it into a decision. -->
Where this stands: the live-annotation path is built but not yet
populated — today the tool runs off a snapshot table of measured values, each row citing the
commit that measured it, because the fleet is mid-migration and no service carries the live
annotation yet. The machinery to read the model live exists; the model is not fully fed. That
is the ordinary shape of real work: the seam is in, the data catches up. What matters for this
chapter is the pattern — a hard metric, measured deterministically, written into a typed model,
and consumed by a tool that turns it into a decision (which path, which service, which lever).

## The flagship: coverage measured one level deeper

Now the middle of the spectrum, and the chapter's teaching example: the "measure one level
deeper" rule applied to the number that most often gets it wrong.

<!-- point: line-coverage-answers-a-syntactic-question-not-the-semantic-one | Line coverage answers a syntactic question — did this line run under some test — which is useful but cannot answer the engineer's actual, semantic question: is the claim this code makes exercised by a test? A file can sit at 95% while the one function implementing a critical invariant is never called, its lines covered incidentally by an unrelated test, so the percentage is true and also a lie about the thing you care about, and the lie is invisible because a percentage has nowhere to put the gap. -->
Line coverage answers a **syntactic** question: did this line run under some test? That is a
fact about source text, and it is genuinely useful. But it cannot answer the question an engineer
actually has, which is **semantic**: is the *claim this code makes* exercised by a test? A file
can sit at 95% line coverage while the one function that implements a critical invariant — call
it INV-18 — is never called by any test at all. Its lines happen to be covered incidentally,
swept up by an unrelated test that imports the module. The 95% is true. It is also a lie about
the thing you care about, and the lie is invisible because a percentage has nowhere to put the
gap.

<!-- point: the-fix-joins-a-traceability-graph-to-the-coverage-oracle | The fix joins two things the system already has: a traceability graph linking each model claim — an invariant, a transition, a service-flow edge — to the code that implements it, pointing at an anchor (the entry-point symbol that carries the claim), and the ordinary coverage oracle that knows which source lines ran; compose them — anchor to its symbol's line range, intersected with the covered-line set — and you can ask whether this claim's code was exercised. -->
The fix comes from joining two things the system already has. The first is a **traceability
graph** that links each model claim — an invariant, a state-machine transition, a service-flow
edge — to the code that implements it. The link points at an **anchor**: the entry point, the
`(path, symbol)` of the function that carries the claim. The second is the ordinary coverage
oracle, which knows exactly which source lines ran. Compose them:

> anchor → its symbol → that symbol's line range → intersect with the covered-line set → *is this model claim's code exercised?*

<!-- point: the-composition-counts-model-claims-and-reports-a-named-verdict | The composition changes the unit of measurement: line coverage counts lines, this counts model claims — the anchors supply the numerator's units, coverage supplies the ground truth of what ran — so the metric reports not a percentage but a named verdict ("this invariant's implementing code is not exercised by any test"), turning an anonymous 87% into an actionable list of the exact model claims the suite walks past. -->
That composition changes the unit of measurement. Line coverage counts lines. This counts
**model claims**. The anchors supply the numerator's units; coverage supplies the ground truth
of what ran. INV-18's anchor resolves to its function, and the function's lines are checked against
what the tests actually executed. The metric then reports not a percentage but a named verdict:
*INV-18's implementing code is not exercised by any test.* An anonymous 87% becomes an
actionable list of the exact model claims your suite walks past.

<!-- point: the-genre-check-names-this-requirements-based-coverage | Run the genre check and this metric has a name — requirements-based coverage, the discipline avionics certification mandates and an open-source requirements tool encodes with git-native items linked to their tests — so the work is not to dream up a bespoke number but to compute the known figure over a graph already built for it, adding the one synthesis the off-the-shelf tools lack: they check a requirement has a linked test, not that the linked test actually executes the requirement's code. -->
This is not a new invention, and it should not be. Run the genre check and the family has a name:
**requirements-based coverage** — the discipline DO-178C mandates for avionics, and the shape the
open-source Doorstop tool encodes with git-native requirement items linked to their tests. The
system's traceability substrate already adopted that schema. So the work here is not to dream up
a bespoke "model coverage" number. It is to compute the requirements-based-coverage figure over a
graph that was already built for it — and to add the one synthesis the off-the-shelf tools lack.
Doorstop checks that a requirement *has* a linked test. It does not check that the linked test
*executes* the requirement's code. Joining the trace edge to the live coverage oracle closes that
last gap.

> Learn more about this governance mechanism: [requirements-based coverage](appendix-b-coverage-model-mapping.html).

<!-- point: one-primitive-supports-several-aggregations-each-a-different-decision | One primitive underneath — resolve an anchor to a line range and intersect it with the covered set — supports several aggregations, each answering a different decision: model-element coverage (which claims that anchor real code go unexercised, the headline fix-list), entry-point coverage (which anchored roots a refactor regression would slip past), and chain coverage (does each claim close end-to-end — code, a test naming it as verifier, and that test actually running the code — the strictest cut that reports low at first and thereby names the verification chains still to build). -->
The single primitive underneath — `anchor_exercised`, which resolves an anchor to a line range
and intersects it with the covered set — supports several aggregations, each answering a
different decision:

- **Model-Element Coverage.** Of the model claims that anchor real code, what fraction is
  exercised? This is the headline: it turns 95%-line-covered into a named list of unexercised
  invariants, each a fix target.
- **Entry-Point Coverage.** Of the code roots the models declare as "changes here mean drift,"
  which are untested? This is the number to watch when refactoring — it tells you which anchored
  roots a regression would slip past.
- **Chain Coverage.** Does each model claim close end-to-end: code exists, a test names it as its
  verifier, *and* that test actually runs the code? This is the strictest cut, and it will report
  low at first, because few claims declare a verifying test today. That low number is the metric
  doing its job, not failing — it names the verification chains still to build.

<!-- point: the-honesty-layer-reports-a-breakdown-so-the-number-affords-engineering | The honesty layer matters as much as the aggregation: a single percentage lies when its denominator is fuzzy, so every figure reports a breakdown — exercised, not-exercised, unmeasurable (anchor unresolvable), out-of-surface (legitimately not coverable code) — and "80%" then reads as what it is, a number that affords the engineering because you can see which part of it you can act on. -->
The honesty layer matters as much as the aggregation. A single percentage lies when its
denominator is fuzzy, so every figure reports a breakdown: exercised, not-exercised, unmeasurable
(the anchor could not be resolved to code), and out-of-surface (a database-tier claim that is
legitimately not code you can cover). "80%" then reads as what it is — *80% of the fifty
measurable claims are exercised; twelve more are unmeasurable and owe a burn-down; eight are out
of surface by design.* That is a number that affords the engineering, because you can see which
part of it you can act on.

<!-- point: measuring-one-level-deeper-cost-a-join-not-a-rebuild | The design is deliberately light: computing all of this requires no change to any model — the anchors already carry their symbol, the covered-line set is a plain read of the coverage database — so the metric is a new consumer of two existing substrates whose expensive parts were already paid for, and measuring one level deeper cost a join, not a rebuild. -->
The design is deliberately light. Computing all of this requires no change to any model — the
anchors already carry their `(path, symbol)`, and the covered-line set is a plain read of the
coverage database the test runner already writes. The metric is a new consumer of two existing
substrates. The expensive parts — the traceability graph, the cross-language coverage oracle —
were already paid for. Measuring one level deeper cost a join, not a rebuild.

## The soft end: doc-derived tests

Slide to the far end of the spectrum, where the metric stops being a number a threshold can act
on and becomes a judgment a human has to make.

<!-- point: a-doc-derived-test-pins-a-doc-claim-and-its-metric-is-genuinely-soft | A doc-derived test pins a behavior stated in a cited document, carrying a trailer that names its source so an edit to the source regenerates the pin; the metric it invites — is this doc-claim pinned by a test? — is genuinely soft, with no clean denominator, because "how many claims does this document make?" is a matter of reading (one reader finds six, another nine, neither wrong) and the set of claims is not enumerable by machine, so the signal points a reader at a document and says "these deserve a pin," it does not decide. -->
A **doc-derived test** pins a behavior stated in a cited document or spec. The test carries a
trailer naming the source it derived from, and when that source is edited, the test is
regenerated so the pin and the prose stay in step. The metric it invites is: *is this
doc-claim pinned by a test?* That question is genuinely soft. It has no clean denominator.
"How many claims does this document make?" is a matter of reading — one reader finds six, another
finds nine, and neither is wrong. You cannot reduce it to a percentage the way you can reduce
line coverage, because the set of claims is not enumerable by machine. The signal points a reader
at a document and says *these behaviors deserve a pin*; it does not decide.

> Learn more about this governance mechanism: [doc-derived test](appendix-c-ddt-pin-trailers.html).

<!-- point: the-soft-metric-is-inseparable-from-a-readiness-judgment-so-a-gate-would-manufacture-hollow-tests | The softness runs deeper than a fuzzy denominator: a doc-derived test is only as good as the seam it tests against, so if decision logic is interleaved with live I/O there is no pure surface to assert against and the test falls back to pinning the source's shape — a test that stays green even when you gut the function's body — which makes the signal inseparable from a judgment about readiness that only a human can answer, and wiring it to a gate that blocks on a threshold manufactures exactly the hollow tests it was meant to prevent. -->
The softness runs deeper than a fuzzy denominator. A doc-derived
test is only as good as the seam it tests against. If the code interleaves its decision logic
with live I/O — a poll loop welded to a queue, a validator welded to a database read — there is
no pure surface to assert a value against. The test then falls back to pinning the *shape* of the
source: that a function exists, that a docstring reads a certain way. That kind of test stays
green even when you gut the function's body. It measures nothing about behavior. So the
doc-derived-test signal is inseparable from a judgment about readiness: are the types sound, is
there a seam to test through, is the contract even documented? Only a human (or an agent
reasoning as one) can answer that, which is what plants this metric firmly at the soft end.
Treat it as hard — wire it to a gate that blocks on a coverage-of-doc-claims threshold — and you
manufacture exactly the hollow tests it was meant to prevent.

## The model runs the machinery

The hard end and the flagship follow the same pattern.

<!-- point: the-model-is-not-a-picture-of-the-machinery-it-is-the-machinery | The models are not documentation that merely stays in sync with the code — they are consumed at runtime to produce the metrics: the cold-start numbers are typed fields a tool reads to derive the longest-path worst case, and the coverage flagship walks the traceability graph to each claim's anchor rather than scanning source for important-looking functions, so in both the model is not a picture of the machinery, it is the machinery — the thing the metric tool reads to know what to measure and how to weigh it. -->
The MBSE models are not documentation that merely stays in sync with the code. They are
**consumed at runtime to produce the metrics.** The cold-start numbers are not a spreadsheet; they
are typed fields on the deployment-topology model, and a tool reads the model to derive the
longest-path worst case. The coverage flagship does not scan source text for functions that look
important; it walks the traceability graph, follows each model claim to its anchor, and measures
*that*. In both, the model is not a picture of the machinery. The model *is* the machinery — the
thing the metric tool reads to know what to measure and how to weigh it.

<!-- point: a-component-distance-grade-is-a-metric-that-drives-a-gate-three-ways | The sharpest instance ties metrics back to mechanisms: a pre-commit gate deciding whether a lint finding should block grades each finding by its distance from the change in the typed component graph — HARD on a touched file (block, the commit's own debt), SOFT in the same component (report and ask, do not block, so concurrent agents do not race the same pre-existing problem), SILENT in a different component (suppress, leaving the whole-tree deploy gate as backstop). -->
The sharpest instance ties metrics back to mechanisms. A pre-commit gate has to decide whether a lint finding
should block a commit. The naive gate blocks on any finding anywhere, which drowns an agent in
pre-existing debt it did not cause. The better gate grades each finding by its **distance from
the change** in the typed component graph, and the grade decides the gate's behavior:

> Learn more about this governance mechanism: [model-graded finding severity](appendix-b-model-graded-finding-severity.html).

- **HARD** — the finding sits on a file this commit touched, or is directly caused by a touched
  input. Block. This is the commit's own debt, and it fails closed.
- **SOFT** — the finding sits in the *same component* as a touched file, but not on a touched
  file. Report it, name it, ask the agent whether the change plausibly caused it — but do not
  block. Blocking here would make concurrent agents race to fix the same pre-existing problem and
  collide.
- **SILENT** — the finding sits in a different component entirely. Suppress it at commit time; the
  whole-tree deploy gate remains the backstop.

<!-- point: the-grade-is-a-metric-read-from-the-model-that-drives-a-sensors-decision | The grade is a metric — a distance read from the model at check time — that drives a sensor's decision directly: the same finding gets three gate behaviors, chosen by consulting the component graph about how far it sits from the change, so here the model is not describing the system for a human to read later, it is queried at the moment of commit to grade the gate — the machinery, run by the model. -->
The grade is a metric — a distance, read from the model at check time — and it drives a sensor's
decision directly. Same finding, three different gate behaviors, chosen by consulting the
component graph about how far the finding is from the change. Here the model is not describing the
system for a human to read later. It is queried, at the moment a commit is made, to grade a gate —
the machinery, run by the model.

## A compact reference

[ref:metrics-table] collects every metric — the decision each drives, where it sits on the hard-soft axis,
and who reads it.

<!-- label: metrics-table -->
<!-- table: The governance metrics at a glance — each metric, the decision it drives, its place on the hard–soft axis, and who reads it. [short: The governance metrics at a glance] -->
| Metric | Decision it drives | Hard / soft | Read by |
|---|---|---|---|
| Per-service cost | Which service to slim, which call to batch | Hard | Cost tooling, admin panel |
| Cold-start latency (per service) | Which service warrants a warm-up lever | Hard | Perf tool, admin panel |
| Cold-start longest-path Σ | Which critical-path chain bounds worst-case latency | Hard | Perf tool, deployment-topology model |
| Model-Element Coverage | Which model claim (invariant, transition, edge) is untested | Flagship (hard number, semantic unit) | Coverage tool, per-model backlog |
| Entry-Point Coverage | Which anchored code root a refactor regression would slip past | Flagship | Coverage tool |
| Chain Coverage | Which requirement lacks an end-to-end verifying test | Flagship | Coverage tool, requirements audit |
| Doc-claim pinning | Which documented behavior still needs a test | Soft | Author, reviewing agent |
| Component-distance grade | Whether a gate blocks, reports, or suppresses a finding | Hard (grade), soft (the causation nudge) | Pre-commit gate, the committing agent |

<!-- point: every-row-names-a-decision-because-a-metric-that-names-none-was-measured-too-shallow | Read the table top to bottom and the spectrum is visible at a glance — stopwatch-hard machine-consumed rows at the top, a hard measurement turned onto a semantic unit in the middle, a reader-aiming judgment at the bottom — and every row names a decision, because a metric that names no decision was measured too shallow, and a loop steering by a shallow number is a loop that cannot steer. -->
Read the table top to bottom and the spectrum is visible in one glance: the top rows are
stopwatch-hard and machine-consumed, the middle rows turn a hard measurement onto a semantic unit,
and the bottom rows aim a reader who still has to judge. Every row names a decision, because a
metric that names no decision was measured too shallow — and a loop steering by a shallow number
is a loop that cannot steer.
