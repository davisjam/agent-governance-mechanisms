*How many recurring failures became infrastructure, and how fast?*

Every recurring failure recognized as a class, converted into a mechanism, then retired from human attention
for good. The reading is the count of conversions and the elapsed time from failure-class to mechanism — the
loop that builds the capital the previous card assesses.

Where engineering capital asks whether the stock is growing, this card shows the engine that grows it: the
failure-to-mechanism-to-retired-forever pipeline.

### What to read

- **Conversions.** How many recurring failure classes became durable mechanisms. Soft-gap — a rich
  loop-and-hypothesis construct with no declared rate metric. The nearest declared proxy is control growth
  (lints 0 to 747, gates 0 to 102): each accreted control is one failure class that stopped recurring.
- **Speed.** The elapsed time from a failure recurring to its class being recognized and converted. Higher
  agentic velocity shortens the time to recognize a class.

```
  GOVERNANCE CONVERSION      "Failures → infrastructure — how many, how fast?"
  ──────────────────────────────────────────────────────────────────────────
        Recurring failures
              |  recognized as one structural class
              v
          Mechanisms           a lint / gate / typed seam / test
              |  converted, then removed from human attention
              v
        Retired forever
  ──────────────────────────────────────────────────────────────────────────
  Proxy for "how many": controls accreted  0 to 747 lints · 0 to 102 gates
```

### The soft-gap read

The **conversion count and rate** is the ratified soft-gap — a rich construct, no declared rate metric. Read
"how many, how fast" green / yellow / red: green when classes convert steadily and recognition time shrinks;
red when the same failures recur un-converted. Anchor it on the control-growth proxy, never a fabricated rate.

### What this projects

The *governance-conversion* concept — the loop by which the environment evolves, each recurring failure
converted into a durable mechanism (see [Chapter 2.3](2.3-the-governed-environment.html)) — plus the
governance-adaptation loop, the failure-class-exposure hypothesis (how fast a class is recognized), and the
conversion-conditions hypothesis (conversions stick when diagnosis capability and change authority sit
together).
