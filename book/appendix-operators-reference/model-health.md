*Are my models still equal to the code?*

This operator card is a projection of the sources below — a one-page reading of a standing operational question. The body is authored on top of this scaffold; nothing here is invented beyond what the sources carry.

### Sources this card projects

- `DASH:model-sync-efficacy` (dashboard metric)
- `DASH:missing-model-metric` (dashboard metric)
- `CAT:models-bridge/system-models/symbol-anchored-traceability-graph` (catalogue mechanism)
- `CAT:models-bridge/system-models/drift-parity-gates` (catalogue mechanism)
- `CAT:models-bridge/system-models/executable-source-of-truth` (catalogue mechanism)
- `CH:3.8-keeping-models-in-sync` (chapter)
