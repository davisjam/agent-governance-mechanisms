<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1120 620" width="1120" height="620" role="group" aria-labelledby="dsl-title dsl-desc" font-family="Georgia, 'Times New Roman', serif">
  <title id="dsl-title">Two producers of one edit language, and the task-to-edit function</title>
  <desc id="dsl-desc">Two producers converge on one shared edit language, which drives one document model. The top producer is the human path: a person working in the editor emits edit operations by hand. The bottom producer is the automated path: a remediation task, such as a figure that needs alt text, becomes an edit operation through a task-to-edit function. Inside that function, the bounded task is packed into a typed input and handed to a model as a typed function call; the model returns a candidate; a deterministic validator checks it before it is allowed through. This is the same LLM-as-function-call shape described earlier in the chapter, applied here. Both producers emit the same closed set of edit operations, and both flow into one document model that applies them and stamps each change. A note marks the task-to-edit mapping as mostly hardcoded today, to be fleshed out, so this is the concept and the trajectory rather than a finished implementation. Two takeaways sit at the bottom. First, routing both producers through one language and one validated boundary buys traceability, every edit is a typed, validated, stamped result you can explain and reverse, and confidence, the deterministic validator holds the line. Second, scoping each call to one bounded task also scopes the reasoning, which lowers token cost. That scoping is the same move as building the whole remediation as a series of small, bounded transformations, seen from the cost angle instead of the pipeline angle.</desc>

  <rect x="0" y="0" width="1120" height="620" fill="#ffffff"/>

  <defs>
    <marker id="dsl-arrow" markerWidth="10" markerHeight="10" refX="7.5" refY="5" orient="auto">
      <path d="M0,0 L10,5 L0,10 z" fill="#2B2416"/>
    </marker>
  </defs>

  <text x="30" y="48" font-size="30" font-weight="bold" fill="#12324f">Two producers, one edit language</text>

  <!-- HUMAN PATH (top) -->
  <rect x="24" y="76" width="270" height="86" rx="3" fill="#eef3f7" stroke="#1a4a7a" stroke-width="1.8"/>
  <text x="159" y="112" text-anchor="middle" font-size="25" font-weight="bold" fill="#12324f">Editor (human)</text>
  <text x="159" y="142" text-anchor="middle" font-size="24" fill="#3d5a70">emits ops by hand</text>

  <!-- AUTOMATED PATH (bottom): task -> function -->
  <rect x="24" y="288" width="240" height="86" rx="3" fill="#eef3f7" stroke="#1a4a7a" stroke-width="1.8"/>
  <text x="144" y="324" text-anchor="middle" font-size="24" font-weight="bold" fill="#12324f">Remediation</text>
  <text x="144" y="352" text-anchor="middle" font-size="24" fill="#3d5a70">task (e.g. alt text)</text>

  <!-- task->edit function box (dashed model inside) -->
  <line x1="264" y1="331" x2="316" y2="331" stroke="#2B2416" stroke-width="1.6" marker-end="url(#dsl-arrow)"/>
  <rect x="320" y="238" width="386" height="196" rx="3" fill="#fbfaf4" stroke="#7a6a2a" stroke-width="1.8"/>
  <text x="513" y="276" text-anchor="middle" font-size="24" font-weight="bold" fill="#5a4a10">task-to-edit function</text>
  <text x="513" y="302" text-anchor="middle" font-size="19" font-style="italic" fill="#7a6a2a">an instance of LLM-as-function-call</text>
  <!-- model (dashed) inside -->
  <rect x="344" y="320" width="160" height="92" rx="3" fill="#FFFBEA" stroke="#BFA45A" stroke-width="1.6" stroke-dasharray="6 4"/>
  <text x="424" y="358" text-anchor="middle" font-size="24" font-weight="bold" fill="#2B2416">Model</text>
  <text x="424" y="386" text-anchor="middle" font-size="19" fill="#6b6250">bounded call</text>
  <!-- validator inside -->
  <rect x="522" y="320" width="160" height="92" rx="3" fill="#eef3f7" stroke="#1a4a7a" stroke-width="1.6"/>
  <text x="602" y="358" text-anchor="middle" font-size="24" font-weight="bold" fill="#12324f">Validator</text>
  <text x="602" y="386" text-anchor="middle" font-size="19" fill="#3d5a70">checks candidate</text>
  <line x1="504" y1="366" x2="520" y2="366" stroke="#2B2416" stroke-width="1.4" marker-end="url(#dsl-arrow)"/>

  <!-- both converge to DSL -->
  <line x1="294" y1="119" x2="736" y2="119" stroke="#2B2416" stroke-width="1.6"/>
  <line x1="736" y1="119" x2="736" y2="164" stroke="#2B2416" stroke-width="1.6" marker-end="url(#dsl-arrow)"/>
  <line x1="706" y1="331" x2="768" y2="331" stroke="#2B2416" stroke-width="1.6" marker-end="url(#dsl-arrow)"/>

  <!-- SHARED DSL -->
  <rect x="632" y="166" width="256" height="80" rx="3" fill="#f3eef7" stroke="#5a3a7a" stroke-width="2"/>
  <text x="760" y="198" text-anchor="middle" font-size="24" font-weight="bold" fill="#3a2350">Shared edit</text>
  <text x="760" y="226" text-anchor="middle" font-size="24" font-weight="bold" fill="#3a2350">language</text>

  <!-- DSL -> document model -->
  <rect x="770" y="292" width="256" height="82" rx="3" fill="#eef3f7" stroke="#1a4a7a" stroke-width="1.8"/>
  <text x="898" y="326" text-anchor="middle" font-size="24" font-weight="bold" fill="#12324f">Document model</text>
  <text x="898" y="354" text-anchor="middle" font-size="19" fill="#3d5a70">applies + stamps each edit</text>

  <!-- hardcoded note -->
  <text x="320" y="466" font-size="19" font-style="italic" fill="#7a5a2a">The task-to-edit mapping is mostly hardcoded today — to be fleshed out.</text>
  <text x="320" y="488" font-size="19" font-style="italic" fill="#7a5a2a">Concept and trajectory, not a finished build.</text>

  <!-- takeaway strip -->
  <line x1="24" y1="506" x2="1096" y2="506" stroke="#c8d6e5" stroke-width="1.4"/>
  <text x="30" y="536" font-size="24" font-weight="bold" fill="#12324f">Traceability and confidence — plus cheaper reasoning</text>
  <text x="30" y="564" font-size="19" fill="#333">One language and one validated boundary make every edit a typed, validated, stamped result you can explain and reverse</text>
  <text x="30" y="588" font-size="19" fill="#333">(traceability), and the deterministic validator holds the line (confidence).</text>
  <text x="30" y="612" font-size="19" fill="#333">Scoping each call to one bounded task also scopes the reasoning — the same move as a series of small transformations, priced.</text>
</svg>
