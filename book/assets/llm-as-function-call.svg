<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1040 430" width="1040" height="430" role="group" aria-labelledby="lfc-title lfc-desc" font-family="'Source Sans 3', -apple-system, 'Segoe UI', sans-serif">
  <title id="lfc-title">The LLM as a typed function call</title>
  <desc id="lfc-desc">A left-to-right pipeline in four stages. A deterministic caller owns the workflow and decides when a step needs judgment. It packs the task into a typed input contract and hands it to the probabilistic component, the model, drawn as a dashed box to mark that its output is not trusted. The model returns a candidate answer that must conform to a typed output contract. A deterministic validator then checks that candidate: on a pass, the result is incorporated and control returns to the caller; on a fail, the caller retries or falls back. The trust boundary sits at the validator, not at the model. The dashed model box and the solid caller and validator boxes together say the same thing the prose does: guidance aims, machinery holds.</desc>

  <rect x="0" y="0" width="1040" height="430" fill="#ffffff"/>

  <defs>
    <marker id="lfc-arrow" markerWidth="9" markerHeight="9" refX="7" refY="4.5" orient="auto">
      <path d="M0,0 L9,4.5 L0,9 z" fill="#1c1917"/>
    </marker>
  </defs>

  <!-- CALLER -->
  <rect x="24" y="150" width="200" height="112" rx="3" fill="#e7edf3" stroke="#2f5169" stroke-width="1.6"/>
  <text x="124" y="182" text-anchor="middle" font-size="24" font-weight="bold" fill="#2f5169">Deterministic</text>
  <text x="124" y="206" text-anchor="middle" font-size="24" font-weight="bold" fill="#2f5169">caller</text>
  <text x="124" y="230" text-anchor="middle" font-size="19" fill="#2f5169">owns the workflow;</text>
  <text x="124" y="252" text-anchor="middle" font-size="19" fill="#2f5169">decides each step</text>

  <!-- input contract -->
  <line x1="224" y1="206" x2="300" y2="206" stroke="#1c1917" stroke-width="1.4" marker-end="url(#lfc-arrow)"/>
  <text x="262" y="184" text-anchor="middle" font-size="18" font-style="italic" fill="#57534e">typed input</text>
  <text x="262" y="200" text-anchor="middle" font-size="18" font-style="italic" fill="#57534e">contract</text>

  <!-- MODEL (probabilistic — dashed) -->
  <rect x="304" y="150" width="200" height="112" rx="3" fill="#faf1e6" stroke="#9a3f12" stroke-width="1.6" stroke-dasharray="6 4"/>
  <text x="404" y="184" text-anchor="middle" font-size="24" font-weight="bold" fill="#1c1917">Model</text>
  <text x="404" y="210" text-anchor="middle" font-size="19" fill="#57534e">the probabilistic</text>
  <text x="404" y="232" text-anchor="middle" font-size="19" fill="#57534e">"function" — one</text>
  <text x="404" y="254" text-anchor="middle" font-size="19" fill="#57534e">bounded task</text>

  <!-- output contract -->
  <line x1="504" y1="206" x2="580" y2="206" stroke="#1c1917" stroke-width="1.4" marker-end="url(#lfc-arrow)"/>
  <text x="542" y="184" text-anchor="middle" font-size="18" font-style="italic" fill="#57534e">typed output</text>
  <text x="542" y="200" text-anchor="middle" font-size="18" font-style="italic" fill="#57534e">contract</text>

  <!-- VALIDATOR -->
  <rect x="584" y="150" width="240" height="112" rx="3" fill="#e7edf3" stroke="#2f5169" stroke-width="1.6"/>
  <text x="704" y="188" text-anchor="middle" font-size="24" font-weight="bold" fill="#2f5169">Deterministic</text>
  <text x="704" y="212" text-anchor="middle" font-size="24" font-weight="bold" fill="#2f5169">validator</text>
  <text x="704" y="240" text-anchor="middle" font-size="19" fill="#2f5169">checks the candidate</text>

  <!-- trust boundary marker under validator; broken around the typed-output-contract label -->
  <line x1="554" y1="116" x2="554" y2="166" stroke="#b23b3b" stroke-width="1" stroke-dasharray="3 3"/>
  <line x1="554" y1="210" x2="554" y2="300" stroke="#b23b3b" stroke-width="1" stroke-dasharray="3 3"/>
  <text x="554" y="108" text-anchor="middle" font-size="18" font-weight="bold" fill="#b23b3b">trust boundary</text>

  <!-- PASS out -->
  <line x1="824" y1="188" x2="876" y2="188" stroke="#1c1917" stroke-width="1.4" marker-end="url(#lfc-arrow)"/>
  <text x="850" y="176" text-anchor="middle" font-size="19" font-weight="bold" fill="#1f7a4d">pass</text>
  <text x="886" y="182" text-anchor="start" font-size="18" fill="#1f7a4d">result</text>
  <text x="886" y="202" text-anchor="start" font-size="18" fill="#1f7a4d">incorporated</text>

  <!-- FAIL loop back to caller -->
  <path d="M704,262 L704,330 L124,330 L124,262" fill="none" stroke="#b23b3b" stroke-width="1.4" stroke-dasharray="5 4" marker-end="url(#lfc-arrow)"/>
  <text x="404" y="350" text-anchor="middle" font-size="19" font-weight="bold" fill="#b23b3b">fail → caller retries or falls back</text>

  <!-- caption strip -->
  <text x="520" y="388" text-anchor="middle" font-size="18" font-style="italic" fill="#57534e">The model is called like a function: a typed task in, a typed answer out —</text>
  <text x="520" y="410" text-anchor="middle" font-size="18" font-style="italic" fill="#57534e">never trusted until a deterministic check has passed it.</text>
</svg>
