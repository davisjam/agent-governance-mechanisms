<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 760 400" width="760" height="400" role="group" aria-labelledby="lfc-title lfc-desc" font-family="Georgia, 'Times New Roman', serif">
  <title id="lfc-title">The LLM as a typed function call</title>
  <desc id="lfc-desc">A left-to-right pipeline in four stages. A deterministic caller owns the workflow and decides when a step needs judgment. It packs the task into a typed input contract and hands it to the probabilistic component, the model, drawn as a dashed box to mark that its output is not trusted. The model returns a candidate answer that must conform to a typed output contract. A deterministic validator then checks that candidate: on a pass, the result is incorporated and control returns to the caller; on a fail, the caller retries or falls back. The trust boundary sits at the validator, not at the model. The dashed model box and the solid caller and validator boxes together say the same thing the prose does: guidance aims, machinery holds.</desc>

  <rect x="0" y="0" width="760" height="400" fill="#ffffff"/>

  <defs>
    <marker id="lfc-arrow" markerWidth="9" markerHeight="9" refX="7" refY="4.5" orient="auto">
      <path d="M0,0 L9,4.5 L0,9 z" fill="#2B2416"/>
    </marker>
  </defs>

  <!-- CALLER -->
  <rect x="24" y="150" width="150" height="100" rx="3" fill="#eef3f7" stroke="#1a4a7a" stroke-width="1.6"/>
  <text x="99" y="182" text-anchor="middle" font-size="14" font-weight="bold" fill="#12324f">Deterministic</text>
  <text x="99" y="200" text-anchor="middle" font-size="14" font-weight="bold" fill="#12324f">caller</text>
  <text x="99" y="222" text-anchor="middle" font-size="11" fill="#3d5a70">owns the workflow;</text>
  <text x="99" y="237" text-anchor="middle" font-size="11" fill="#3d5a70">decides each step</text>

  <!-- input contract -->
  <line x1="174" y1="200" x2="252" y2="200" stroke="#2B2416" stroke-width="1.4" marker-end="url(#lfc-arrow)"/>
  <text x="213" y="185" text-anchor="middle" font-size="10.5" font-style="italic" fill="#5a5240">typed input</text>
  <text x="213" y="197" text-anchor="middle" font-size="10.5" font-style="italic" fill="#5a5240">contract</text>

  <!-- MODEL (probabilistic — dashed) -->
  <rect x="256" y="150" width="150" height="100" rx="3" fill="#FFFBEA" stroke="#BFA45A" stroke-width="1.6" stroke-dasharray="6 4"/>
  <text x="331" y="182" text-anchor="middle" font-size="14" font-weight="bold" fill="#2B2416">Model</text>
  <text x="331" y="199" text-anchor="middle" font-size="11" fill="#6b6250">the probabilistic</text>
  <text x="331" y="214" text-anchor="middle" font-size="11" fill="#6b6250">"function" — one</text>
  <text x="331" y="229" text-anchor="middle" font-size="11" fill="#6b6250">bounded task</text>

  <!-- output contract -->
  <line x1="406" y1="200" x2="484" y2="200" stroke="#2B2416" stroke-width="1.4" marker-end="url(#lfc-arrow)"/>
  <text x="445" y="185" text-anchor="middle" font-size="10.5" font-style="italic" fill="#5a5240">typed output</text>
  <text x="445" y="197" text-anchor="middle" font-size="10.5" font-style="italic" fill="#5a5240">contract</text>

  <!-- VALIDATOR -->
  <rect x="488" y="150" width="150" height="100" rx="3" fill="#eef3f7" stroke="#1a4a7a" stroke-width="1.6"/>
  <text x="563" y="188" text-anchor="middle" font-size="14" font-weight="bold" fill="#12324f">Deterministic</text>
  <text x="563" y="206" text-anchor="middle" font-size="14" font-weight="bold" fill="#12324f">validator</text>
  <text x="563" y="228" text-anchor="middle" font-size="11" fill="#3d5a70">checks the candidate</text>

  <!-- trust boundary marker under validator -->
  <line x1="466" y1="120" x2="466" y2="290" stroke="#a33" stroke-width="1" stroke-dasharray="3 3"/>
  <text x="466" y="112" text-anchor="middle" font-size="10.5" font-weight="bold" fill="#a33">trust boundary</text>

  <!-- PASS out -->
  <line x1="638" y1="185" x2="712" y2="185" stroke="#2B2416" stroke-width="1.4" marker-end="url(#lfc-arrow)"/>
  <text x="675" y="176" text-anchor="middle" font-size="11" font-weight="bold" fill="#2a6b2a">pass</text>
  <text x="722" y="182" text-anchor="start" font-size="11" fill="#2a6b2a">result</text>
  <text x="722" y="196" text-anchor="start" font-size="11" fill="#2a6b2a">incorporated</text>

  <!-- FAIL loop back to caller -->
  <path d="M563,250 L563,320 L99,320 L99,250" fill="none" stroke="#a33" stroke-width="1.4" stroke-dasharray="5 4" marker-end="url(#lfc-arrow)"/>
  <text x="331" y="337" text-anchor="middle" font-size="11" font-weight="bold" fill="#a33">fail → caller retries or falls back</text>

  <!-- caption strip -->
  <text x="24" y="372" font-size="11.5" font-style="italic" fill="#555">The model is called like a function: a typed task in, a typed answer out, and never trusted until a deterministic check has passed it.</text>
</svg>
