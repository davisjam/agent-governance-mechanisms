{
  "_note": "Outcomes-projection SELECTION — the SSOT for WHICH learning outcomes the site surfaces and how each is realized on the landing. This file is deliberately THIN: it does NOT copy outcome prose (that would drift). It declares only (a) the SELECTION policy — which outcomes from the book's outcomes view (book-models/outcomes.json) the site projects — and (b) the per-outcome SITE realization + book-home link, keyed by the outcome's `outcome_id`. The build reads outcomes.json, applies this selection, and renders each outcome's `statement` + `bloom` + `verb`/`obj` STRAIGHT FROM the model — so re-running the outcomes drain re-syncs the site automatically. The drift check (`check_outcomes_site_*` in tests/html.py) joins on `outcome_id` and asserts: (a) every selected id resolves to a real outcome in outcomes.json (no dangling selection), and (b) every book/Part outcome the policy WOULD select is either projected or explicitly excluded here (no silently-dropped core outcome). Lands AUDIT-ONLY-first. Queryable via `catalog.py outcomes-site`.",

  "_note_landing_drop": "The landing dropped its on-landing outcomes strip (commit 727378a — 'end the landing at the nav cards; drop on-landing catalogue + vocabulary strip'). The site now projects NO outcomes: `granularity_in` is empty, so no outcome is policy-eligible; `projected` and `_excluded` are empty. `check_outcomes_site` then passes vacuously — no dangling projected id, nothing policy-eligible to demand projection, no rows to find on the landing. This file is retained as the book-models selection sidecar (the realization is the book's own outcomes render), not deleted, so the projection can be re-enabled if the landing ever re-adds a compact outcomes preview.",

  "_selection_policy": {
    "granularity_in": [],
    "provenance_not_in": ["gap-recommended"],
    "note": "Landing outcomes strip dropped (727378a): `granularity_in` emptied so NO outcome is policy-eligible for site projection. The prior policy surfaced BOOK- and PART-level DERIVED/DECLARED outcomes; restore `[\"book\", \"part\"]` here (and repopulate `projected`) only if the landing re-adds an on-landing outcomes preview."
  },

  "_site_home_section": "outcomes-view",
  "_site_home_note": "All projected outcomes render inside one landing section id='outcomes-view' ('What you'll be able to do'). Individual outcome rows carry id='outcome-<slug>' derived from the outcome_id, so each is deep-linkable and the drift check can join site->model per row.",

  "_book_home_map": {
    "book": "book/index.html",
    "part-1": "book/1.1-the-printer.html",
    "part-2": "book/2.1-the-agent-stack.html",
    "part-3": "book/3.1-the-executable-zoo.html",
    "part-4": "book/4.1-brownfield.html",
    "part-5": "book/5.1-the-ada-context.html",
    "part-6": "book/6.0-implications-for-se.html",
    "_note": "book-home link per primary_unit — book-level outcomes point at the book index; a Part outcome points at that Part's first chapter (book/index.html has no per-part anchor). Derived from book-models/outline.json (part -> first chapter slug); regenerate if the book's part->first-chapter mapping changes."
  },

  "_excluded": [],

  "projected": []
}
